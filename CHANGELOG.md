- Se implementó `Shifter.hdl` para corrimiento de bits a izquierda y derecha.
- Se corrigió un error inicial en el sentido del corrimiento.
- Se agregó la salida `result` para el bit expulsado.

- Se actualizó `ALU.hdl` para reconocer el comando de shift.
- Se validaron los ejemplos esperados:
  - `D=M<<1`
  - `AM=D>>1`
    
- Se corrigió una conexión de `pc` que causaba saltos incorrectos.
  
- Se agregó `design.txt` con el formato C.

- Del proyecto 5 se completaron:
  - `CPU.hdl`
  - `Memory.hdl`
  - `Computer.hdl`




